<%= render "header" %>

<main> 
  <h1>Event Overview</h1>
  <% @events.each do |event| %>
    <div class="event">
      <h2><%= event.name %></h2>
      <p><%= event.location %></p>
      <p><%= event.date %></p>
    </div>
  <% end %>
  
</main>
